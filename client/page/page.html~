<template name="page">
<div class="page">
<h1 class="pageTitle">{{title}}</h1>

{{#if isSelectedPage "home"}}
  {{> pageHome}}
{{else}}
  {{#if isSelectedPage "market"}}
    {{> pageMarket}}
  {{else}}
    {{#if isSelectedPage "market"}}
      {{> pageMarket}}
    {{else}}
      {{#if isSelectedPage "market"}}
        {{> pageMarket}}
      {{else}}
        {{#if isSelectedPage "market"}}
          {{> pageMarket}}
        {{else}}
          {{#if isSelectedPage "market"}}
            {{> pageMarket}}
          {{else}}
            {{! content}}
            Page not found!!
          {{/if}}
        {{/if}}
      {{/if}}
    {{/if}}
  {{/if}}
{{/if}}

</div>


</template>
